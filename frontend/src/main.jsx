import React from 'react'
import ReactDOM from 'react-dom/client'
import ComicGrader from './ComicGrader'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ComicGrader />
  </React.StrictMode>,
)
